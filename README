Since OJDBC jar is not in central maven repository, please use the uploaded JAR in the application and run the following command to install it in the local maven repository

mvn install:install-file -Dfile=ojdbc8.jar -DgroupId=com.oracle -DartifactId=ojdbc8 -Dversion=1.0 -Dpackaging=jar
